apiVersion: 1

datasources:
  - name: PostgreSQL
    type: postgres
    access: proxy
    url: db:5432
    database: smarthome
    user: postgres
    secureJsonData:
      password: postgres
    isDefault: true
    editable: true
